<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mywallet">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyWallet">
        <activity android:name=".activities.Balance"></activity>
        <activity android:name=".activities.ContactUs" />
        <activity android:name=".activities.RateUs" />
        <activity android:name=".activities.ForgetPassword" />
        <activity android:name=".activities.Home" />
        <activity android:name=".activities.SignUp" />
        <activity android:name=".activities.ExpensesCategory" />
        <activity android:name=".activities.IncomeCategory" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>